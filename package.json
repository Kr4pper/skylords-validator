{
  "name": "skylords-validator",
  "version": "0.1.0",
  "description": "validator for skylords game data",
  "author": "Kr4pper",
  "main": "index.js",
  "scripts": {
    "build": "rmdir /s /q dist && tsc -b",
    "validate": "node dist/index.js --db E:\\dev\\skylords\\jsondb",
    "restart": "npm run build && cross-env LOG_LEVEL=debug npm run validate",
    "parseCards": "node dist/util/parse-card-ids.js",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  }
}
